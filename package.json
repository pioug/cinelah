{
  "name": "cinelah",
  "scripts": {
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "rm -rf public && cd site && npm run build && cd .. && firebase deploy --only hosting"
  },
  "dependencies": {
    "eslint": "3.19.0",
    "eslint-config-bandlab-base": "1.5.0",
    "eslint-plugin-react": "7.0.1",
    "eslint-plugin-unicorn": "2.1.1",
    "firebase-tools": "3.9.0"
  }
}
