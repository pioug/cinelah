{
  "name": "cinelah",
  "scripts": {
    "deploy:functions": "cd nuxt-server && npx nuxt build && mv .nuxt ../functions/.nuxt && cp nuxt.config.js ../functions && cp -a static ../functions && cd .. && firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase-tools": "7.7.0"
  },
  "devDependencies": {
    "eslint": "6.6.0",
    "eslint-plugin-react": "7.16.0",
    "eslint-plugin-vue": "6.0.1",
    "prettier": "1.19.1"
  }
}
