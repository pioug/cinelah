{
  "name": "cinelah",
  "scripts": {
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "rm -rf public && cd site && npm run build && cd .. && firebase deploy --only hosting"
  },
  "dependencies": {
    "eslint": "5.0.1",
    "eslint-config-bandlab-base": "2.3.0",
    "eslint-plugin-react": "7.10.0",
    "eslint-plugin-unicorn": "4.0.3",
    "firebase-tools": "3.19.3"
  }
}
